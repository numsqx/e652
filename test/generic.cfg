MEMORY {
  E:  start = $0000, size = $10000, fill = yes;
}

SEGMENTS {
  ZEROPAGE:   load = E, type = zp, start = $0000;
  DATA:       load = E, type = rw, start = $0200;
  CODE:       load = E, type = ro, start = $0400;
  VEC:        load = E, type = ro, start = $FFFA;
}
